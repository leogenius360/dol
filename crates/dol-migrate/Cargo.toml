[package]
name = "dol-migrate"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Explicit schema catalog inspection, diffing, and migration planning for DOL"

[dependencies]
dol-core = { path = "../dol-core", version = "0.1.0" }
dol-engine = { path = "../dol-engine", version = "0.1.0" }

[lints]
workspace = true
