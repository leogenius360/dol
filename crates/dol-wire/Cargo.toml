[package]
name = "dol-wire"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Bounded, versioned DOL interchange boundary"

[dependencies]
dol-core = { path = "../dol-core", version = "0.1.0" }
time = { version = "0.3", features = ["std"] }

[lints]
workspace = true
