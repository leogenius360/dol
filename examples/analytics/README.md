# Analytics examples

Aggregation and deterministic window authoring are covered by the executable pipeline suites in `tests/pipelines_slice3.rs` and `tests/pipelines_slice4.rs`; `cargo xtask adoption` compiles the public facade examples.
