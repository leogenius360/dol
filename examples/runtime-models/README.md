# Runtime-models examples

Runtime model construction and dense-row validation are exercised by `crates/dol-core/tests/phase1.rs`; static and runtime definitions use the same semantic fingerprints and engine paths.
